"""Rule engine skeleton for incident signal detection."""

from typing import Dict, List, Any


def apply_rules(events: List[Dict[str, Any]], rules: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
    """Apply rule set to normalized events (placeholder)."""
    _ = events, rules
    return []
