"""Kubernetes event collection helpers (scaffold)."""

from typing import List, Dict


def collect_events(namespace: str) -> List[Dict[str, str]]:
    """Return normalized events for a namespace (placeholder)."""
    _ = namespace
    return []
